/**
 * @fileOverview Manages route definition creation.
 * Exposes method to create a route definition object, that shall be used to
 * render routes.
 * Should probably use flow.
 * 
 * The idea is to have route definitions, which means an object that represents
 * a single route, instead of hard-coded strings scattered through out the
 * code-base, or just simple constants.
 */
